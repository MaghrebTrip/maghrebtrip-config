service:
  port: ${services.tourist.port}

eureka-server:
  url: http://${services.eureka-server.host}:${services.eureka-server.port}/eureka

db:
  url: jdbc:postgresql://${db.host}:${db.port}/tourist
