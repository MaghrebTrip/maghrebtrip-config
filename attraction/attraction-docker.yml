service:
  port: ${services.attraction.port}

eureka-server:
  url: http://${services.eureka.host}:${services.eureka-server.port}/eureka

db:
  url: jdbc:postgresql://${db.host}:${db.port}/attraction
