service:
  port: ${services.attraction.port}

eureka-server:
  url: http://${services.eureka-server.host}:${services.eureka-server.port}/eureka

db:
  url: jdbc:mysql://${db.host}:${db.port}/attraction?sessionVariables=sql_mode='NO_ENGINE_SUBSTITUTION'&jdbcCompliantTruncation=false
