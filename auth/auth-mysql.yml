service:
  port: ${services.auth.port}

eureka-server:
  url: http://${services.eureka.host}:${services.eureka-server.port}/eureka

db:
  url: jdbc:mysql://${db.host}:${db.port}/auth?sessionVariables=sql_mode='NO_ENGINE_SUBSTITUTION'&jdbcCompliantTruncation=false
