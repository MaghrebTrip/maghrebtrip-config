service:
  port: ${services.city.port}

eureka-server:
  url: http://${services.eureka-server.host}:${services.eureka-server.port}/eureka

db:
  url: jdbc:mysql://${db.host}:${db.port}/city?sessionVariables=sql_mode='NO_ENGINE_SUBSTITUTION'&jdbcCompliantTruncation=false
